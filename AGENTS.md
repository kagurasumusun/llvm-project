# AGENTS.md

## Project Context

## Mission Priority
- 正確性 > セキュリティ > 保守性・整合性 > パフォーマンス > 簡潔さ。判断が割れた場合はこの順序で決める。
- 完了基準は「動くコードが出力された」ではなく「テストが通り、Definition of Doneを満たしたPRが出た」こと。

## Operating Principles(すべてのタスク共通)
- 実装前に必ず、最低2つの実装方針を比較し、選んだ理由(可読性/性能/影響範囲/将来の拡張性のトレードオフ)をプランに明記すること。安易に最初に思いついた方法へ飛びつかない。
- 実装前に「このタスクでありがちな落とし穴」を自分で洗い出してからコードを書くこと(null/undefined、境界値、空データ、巨大入力、並行処理、権限漏れ、既存機能への副作用、タイムゾーン/エンコーディング、型強制、リソースリークなど)。
- 記憶に自信がないAPI仕様・ライブラリのバージョン挙動・破壊的変更は、リポジトリ内のコードやドキュメント、あるいは公式ドキュメントで確認してから使う。存在しないAPI・関数・オプションを推測で書かない。確認できなかった場合はPR説明にその旨を明記する。
- 既存コードのスタイル・命名・設計パターンと矛盾する変更をしない。矛盾せざるを得ない場合はプランとPR説明で理由を明示する。
- タスクの範囲が曖昧な場合は、止まって質問するより先に、最も妥当な解釈を1〜2行で明示した上でプランを提示する。根本的に方向性が割れる場合のみ質問する。
- 実装者視点だけでなく、レビュアー視点・攻撃者視点・運用者視点の最低3視点で自分の変更を見直してから提出する。
- 「動いたように見える」で終わらせず、なぜ動くと確信できるかの根拠(テスト結果・ログ・手動確認手順)を示す。

## Architecture & Design Principles
- 単一責任・関心の分離を守り、既存のレイヤー構造を横断する変更は最小限にする。
- 車輪の再発明をしない。既存のユーティリティ・共通処理があれば流用し、重複実装を作らない。
- YAGNI: 要求されていない汎用化・将来のための抽象化を先回りして作らない。ただし既存の拡張ポイントは尊重する。
- 循環依存・密結合を作らない。新しい依存関係を追加する場合は方向性を明記する。
- 破壊的変更(API/スキーマ/公開インターフェース)を行う場合は、影響範囲・移行手順・後方互換性の有無を必ずPR説明に書く。

## Coding Standards
- コメントは「なぜそうしたか」を書く。「何をしているか」の説明コメントは書かない。
- 関数・メソッドは単一の責務に絞り、肥大化したら分割する。
- マジックナンバー・マジックストリングは避け、意味のある名前を持つ定数にする。
- エラーを握りつぶさない(空catch禁止)。呼び出し元が判断できる形で伝播させるか、握りつぶす場合は理由をコメントで明示する。
- 公開関数・API・エクスポートされるモジュールには入出力の型と契約を明示する。
- 未使用のコード・デッドコード・コメントアウトされた旧実装を残さない。
- デバッグ用のprint/console.log/一時的なコードをコミットに残さない。

## Language & Toolchain Standards

### Version Detection Protocol(言語・ツールチェーンを触る全タスクで最初に必ず実施)
- 実装前に、対象プロジェクトが実際に使っているバージョンを特定する。最新版を前提にしない。特定できる手がかりの例:
  - LLVM/Clang本体: 対象ブランチ・リリースタグ(`release/X.x`、特定コミット)、`LLVM_VERSION`、CIのツールチェーン指定
  - Clangでコンパイルされる側のプロジェクト: `clang --version`、`compile_commands.json`、CI設定、`CMakeLists.txt`の`CMAKE_C_STANDARD`/`CMAKE_CXX_STANDARD`、`-std=`フラグ
  - Metal: プロジェクトの`MTLLanguageVersion`ビルド設定、`.metal`ファイルの`-std=metalX.Y`指定、サポート対象最小`MTLGPUFamily`/OSバージョン
  - Swift: `Package.swift`の`swift-tools-version`、Xcodeの`SWIFT_VERSION`、既存コード中の`#if swift(>=X)`分岐
  - Objective-C: `-fobjc-arc`有無、Deployment Target
- 特定できた版数より新しい構文・API・フラグ・警告・サニタイザは使わない。特定できない場合は、確認できる範囲で最も古い(保守的な)想定で実装し、その前提をPR説明に明記する。
- 「最新版なら使える機能」を無条件に使わず、必ず対象バージョンでの可用性を確認してから使う(自信がなければ検索・リリースノート・仕様書で確認する)。
- 同一リポジトリ内に複数バージョン/複数ターゲットが混在する場合(古いブランチ向けバックポート、複数Xcode世代対応、複数GPUファミリ対応など)、最も制約が厳しいバージョンに合わせて実装するか、条件分岐で書き分ける。

### LLVM / Clang(特定の版に依存しない書き方)
- **LLVM/Clang本体の開発(コンパイラへのパッチ、パス、フロントエンド/バックエンド拡張)の場合**: 対象ブランチ・リリース系列を確認し、そのバージョンのAPI形状(例: Legacy Pass Manager時代かNew Pass Manager移行後か、`opt`/`llc`のオプション名、`clang::`内部APIの当時の形)に合わせる。当時存在しないAPI・ヘッダ・ユーティリティを後の版から持ち込まない。ビルド・テストはそのバージョンの`llvm-lit`/`check-llvm`/`check-clang`手順に従う。
- **Clangでコンパイルされる側のプロジェクトの場合**: 対象C/C++標準(C89〜C23、C++98〜C++26のどれか)を判定してから、それより新しい標準の構文・属性・標準ライブラリ機能を使わない。新しめの機能は`__has_feature`/`__has_cpp_attribute`/`__has_builtin`/`#if __cplusplus >= ...`でガードし、対象範囲外のビルドを壊さない形にする。
- 警告フラグ・サニタイザ(ASan/UBSan/TSan)・`clang-tidy`のチェック名は版によって存在有無・既定挙動が異なるため、対象Clangバージョンで実在するオプションかを確認してから使う。存在しない場合は、そのバージョンで可能な代替手段(手動レビュー、既存の静的解析ツール)を明記する。
- ABI(Itanium C++ ABI、libstdc++/libc++の世代)に関わる変更は、対象バージョン・対象プラットフォームでの互換性を明記する。
- LLVM IRを触るタスクでは、対象バージョンのIR文法・パス構成での中間出力確認、既存パスとの相互作用、SSA形式の不変条件を壊していないかを検証してからPRを出す。

### Apple Metal / MSL / AIR / metallib(Metal 1〜最新のいずれにも対応)
- MSLのベースC++バージョン(世代によりC++14ベース/C++17ベース)や`-std=metalX.Y`は、プロジェクトの`MTLLanguageVersion`/ビルド設定から判定する。判定できなければ最も後方互換性の高い設定を前提にする。
- MSL仕様上、バージョンを問わず共通で非対応の機能(ラムダ式、動的キャスト、RTTI、`new`/`delete`、例外処理、仮想関数、派生クラス)は使わない。それとは別に、特定の世代以降でのみ追加された機能(引数バッファの新世代、テンソル型、統合コマンドエンコーダ等の新API)は対象MSLバージョン/GPUファミリで実際に使えるかを確認してから使う。
- 対象GPUファミリ(`MTLGPUFamily`)を明示し、対象範囲外のデバイス・OSバージョンで使えない機能に無条件で依存しない。古いデバイスもサポート対象なら、機能ごとのフォールバックパスを用意する。
- コンパイルパイプライン(`.metal`→`.air`→`.metallib`)のツール(`xcrun metal`/`metallib`)はXcode/Metalツールチェーンの世代でオプション名・既定値が変わりうるため、プロジェクトが使う実際のツールチェーンバージョンに合わせる。
- パフォーマンスはスレッドグループサイズ・occupancy・レジスタ圧・スレッドグループメモリ使用量を意識し、対象世代で使えるプロファイリングツール(GPU Frame Capture等)による実測結果をPR説明に含める。
- レンダリング結果の再現性に関わるコンパイルオプション(浮動小数点縮約の有無、invariant指定など)を変更する場合、視覚的な差分が出うることをPR説明に明記する。

### C / C++(C89〜C23、C++98〜C++26のどれにも対応)
- 対象標準はビルド設定から判定し、それより新しい機能(例: C++11より前のプロジェクトで`auto`推論・範囲for・スマートポインタ、C++20より前で`concepts`/`ranges`/コルーチン)を使わない。
- 古い標準が対象の場合、RAII/スマートポインタが使えないことがあるため、既存の所有権管理パターン(手動`delete`、参照カウント実装、ownerを明示する命名規約など)に合わせつつ、リーク・二重解放・ダングリングポインタを避ける設計にする。
- 未定義動作(符号変換、配列境界外アクセス、データ競合、ストリクトエイリアシング違反)の排除は標準の新旧に関わらず最優先する。
- コンパイラ拡張(GNU拡張、MSVC拡張、旧バージョン固有の`__attribute__`/`#pragma`)への依存は、対象コンパイラ・バージョンで明示的にサポートされているかを確認する。
- 公開ヘッダのABI/API互換性は、対象プロジェクトが約束しているバージョニング方針(シンボルバージョニング、インラインネームスペース、あるいは互換性を約束していないなら明記)に従う。

### Swift(Swift 1系〜最新の言語モードいずれにも対応)
- 対象Swift言語モード/バージョンは`Package.swift`の`swift-tools-version`やXcodeの`SWIFT_VERSION`から判定する。
- バージョン依存機能(`async`/`await`、Strict Concurrency、`Sendable`、ノンコピー型など)は対象バージョンで利用可能か確認してから使う。利用できない古いバージョンが対象なら、GCD/コールバックベースなど当時の慣用パターンに合わせる。
- 新しい言語機能を使う場合は`#if swift(>=X.Y)`でガードし、対象範囲外のビルドを壊さない。
- Optionalの強制アンラップ(`!`)を避け安全に扱う方針、C/Objective-Cとの境界でのメモリ管理主体の明記は、バージョンによらず共通のルールとする。

### Objective-C / Objective-C++(ARC以前・以後いずれにも対応)
- ARCが有効か(`-fobjc-arc`)、対象Deployment TargetがARCをサポートするかを確認する。ARC無効(MRC)なプロジェクト/ファイルでは、対象コードの`retain`/`release`/`autorelease`管理パターンに合わせる。ARC有効なら手動でretain/releaseを書かない。
- ブロックのretain cycle回避(`weak self`等)やSwift向けnullability注釈は、対象Deployment Targetでサポートされている範囲でのみ使う。
- Objective-C++(`.mm`)でC++例外とObjective-C例外を混在させる場合、対象プラットフォーム・対象コンパイラ世代でのスタック巻き戻し挙動を確認する。

### Cross-Language Interop / バージョン境界
- Swift ⇄ Objective-C ⇄ C/C++の境界をまたぐ変更は、どちら側がメモリ・スレッド安全性の責任を持つかを明記する。
- 一部だけ古いツールチェーン(古いXcode、古いLLVM派生ブランチ等)でビルドされるターゲットが混在する場合、その差異を検知し、条件付きコンパイルまたはターゲットごとの別実装方針を明記する。
- ビルドシステム(CMake/Swift Package Manager/Xcodeプロジェクト)をまたぐ変更は、対象となる全バージョン・全ターゲットでビルド・リンクが通ることを確認してからPRを出す。

## Security Checklist
- 外部入力(ユーザー入力・APIレスポンス・ファイル・環境変数)は信頼せず、境界で検証・サニタイズする。
- SQL/コマンド/パス/テンプレートインジェクションを防ぐため、文字列結合でクエリやコマンドを組み立てない。
- 認証・認可のチェックを迂回できる経路がないか確認する。権限チェックはサーバ側/信頼できる境界で行う。
- 秘密情報(APIキー・トークン・パスワード)をコード・ログ・エラーメッセージ・コミット履歴に含めない。
- 依存ライブラリを追加・更新する場合、既知の脆弱性がないか確認する。
- 機密データはログに平文で出力しない。

## Performance & Reliability Checklist
- ループ内でのDBクエリ・API呼び出し(N+1問題)を作らない。
- 明らかに悪い計算量(不要なO(n²)以上)を避ける。データ量が増えた場合の挙動を意識する。
- 開いたリソース(ファイル・コネクション・ロック)は確実に解放する。
- 外部呼び出しにはタイムアウトを設定し、無限待ちを作らない。
- 冪等性が必要な処理(リトライされうる処理)は冪等に設計する。
- 並行処理・非同期処理では競合状態・デッドロックの可能性を検討する。

## Testing Requirements(Definition of Doneの一部)
- 変更に対応するテストを必ず追加または更新すること。ハッピーパスだけでなく異常系・境界値も含める。
- PRを作成する前に、関連する既存テストと新規テストを実際に実行し、全て通ることを確認すること。テストを実行せずに「動くはず」で終わらせない。
- テストが書けない・実行できない理由がある場合は、PR説明にその理由と代替の検証方法(手動確認手順など)を明記すること。
- フレーキー(不安定)なテストを新たに作らない。外部依存・時刻・乱数はテスト内で制御する。
- バグ修正の場合、そのバグを再現する回帰テストを必ず追加する。

## Debugging Protocol(バグ修正タスク時)
1. 再現条件を特定する。再現できない場合はその旨を明記する。
2. スタックトレース・ログ全体から根本原因を特定する。表面的な症状だけ消す修正をしない。
3. 直近の変更履歴(git log/blame)を確認し、いつから壊れたかを特定できないか試す。
4. 修正前に仮説を立て、最小限のログ/テストで仮説を検証する。
5. 修正後、関連する既存機能にリグレッションがないか確認する。
6. 再発防止(テストケース追加、型/バリデーション強化)を合わせて提案する。
7. 最初の仮説が外れた場合、同じアプローチに固執せず別の仮説に切り替える。3回以上同じ方向で失敗したら、方針転換の必要性自体をPR説明/コメントで明示する。

## Refactor & Large Change Protocol
- 大規模な変更は、可能な限り小さく検証可能な単位に分割して段階的に進める。
- 挙動を変えないリファクタリングと、挙動を変える変更を同じコミット/PRに混在させない。
- 破壊的な移行が必要な場合、フィーチャーフラグや段階的移行(旧実装を残しつつ新実装に寄せる)を検討する。
- スキーマ変更を伴う場合、マイグレーションの前方・後方互換性を確認する。

## Git / PR Hygiene
- 1つのPRは1つの目的に絞る。無関係な変更(フォーマットの一括変更など)を混ぜない。
- コミットメッセージは変更内容と理由が分かるように書く。
- PR説明には: 変更概要 / 選んだ理由と検討した代替案 / 検証方法と結果 / 既知のリスクや未対応点 を必ず含める。

## Failure / Escalation Protocol
- 完了条件を満たせなかった場合、満たしたふりをしたPRを作らない。何ができて何ができなかったかを正直に報告する。
- テスト失敗・環境不備等でブロックされた場合、原因の切り分け結果を具体的に報告する(単に「失敗した」で終わらせない)。
- 要求そのものに矛盾・実現不可能な制約がある場合、実装を強行せず、矛盾点を指摘した上で妥当な代替案を提示する。

## Definition of Done(PRを開く前の自己チェック)
- [ ] 要求仕様とエッジケースを満たしている
- [ ] 既存コードとスタイル・設計が整合している
- [ ] 変更した言語・ツールチェーン(LLVM/Clang、Metal/MSL/AIR/metallib、C/C++、Swift、Objective-C)固有のLanguage & Toolchain Standardsを満たしている
- [ ] Security Checklistに問題がない
- [ ] Performance & Reliability Checklistに問題がない
- [ ] 関連テスト(異常系・回帰テストを含む)を実行し、全て通過した
- [ ] デッドコード・デバッグ出力・不要な変更が残っていない
- [ ] 秘密情報がコード・ログ・コミットに含まれていない
- [ ] PR説明に: 変更概要 / 選んだ理由と検討した代替案 / 検証方法と結果 / 既知のリスクや未対応点 を記載した

## Custom Agents / 主要ディレクトリ

## Secrets / Environment
- 本番用のシークレットやAPIキーはこのリポジトリにもAGENTS.mdにも記載しない。テスト・ビルドに必要な認証情報はJulesのSecret Store経由でスタブ値/ローカル専用の値として渡すこと。
- 依存関係のインストール手順が特殊な場合はEnvironment Snapshotとして保存し、毎回のVM起動を安定させる。
- 認証情報は必要最小限の権限スコープで発行し、ログに出力しない。
